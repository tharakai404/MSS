using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace ABC.Services
{

    public class LogService
    {
      /*  private Apartment_managementEntities2 db = new Apartment_managementEntities2();
        
        public void SaveLog(string modulename,string action, string loginuser)
        {
            UserLog userLog = new UserLog();
            userLog.Date = DateTime.Now; 
            userLog.User_name = loginuser;
            userLog.Description = modulename + " "+action + " by " + loginuser;
            db.UserLog.Add(userLog);
            db.SaveChanges();
        }*/
    }
}